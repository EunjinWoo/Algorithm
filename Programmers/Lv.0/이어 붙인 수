function solution(num_list) {
    const even_list = [];
    const odd_list = [];
    num_list.forEach((a) => {a%2===0 ? even_list.push(a) : odd_list.push(a)});
    return Number(even_list.join(''))+Number(odd_list.join(''));
}
